<Window x:Class="EncriptarClave.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EncriptarClave"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,10">
            <Label Content="Ingrese la clave:" FontWeight="Bold" FontSize="16"/>
            <TextBox x:Name="txtClave" Height="35" FontSize="16"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">

            <Button x:Name="btnEncriptar" Content="Encriptar" Width="120" Height="40" Margin="10" Click="btnEncriptar_Click"/>

            <Button x:Name="btnDesencriptar" Content="Desencriptar" Width="120" Height="40" Margin="10" Click="btnDesencriptar_Click"/>

        </StackPanel>


        <StackPanel Grid.Row="2">

            <Label Content="Resultado:" FontWeight="Bold" FontSize="16"/>

            <TextBox x:Name="txtResultado" Height="35" FontSize="16" IsReadOnly="True"/>

        </StackPanel>

    </Grid>

</Window>
